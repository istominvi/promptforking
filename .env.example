# Supabase (project: dashboard → Settings → API Keys)
# NEXT_PUBLIC_SUPABASE_ANON_KEY can be either the legacy `anon` JWT or the new `sb_publishable_*` key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Provider API keys — for local dev/test only. NOT deployed to Vercel.
REPLICATE_API_TOKEN=
FAL_API_KEY=
